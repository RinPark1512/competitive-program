// Author: Catherine Park
// It is ok to share my code anonymously for educational purposes
#include <iostream>
#include <algorithm>
#include <vector>

using namespace std; 
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    // calculate speed w/o sheilds, take max
    // take speed, calculate position w/ sheilds, if works or not
    long long shipX, shipY, n;
    cin >> shipX;
    cin >> shipY;
    cin >> n;
    for (long long i = 0; i < n; i++) {
        long long temp;
        
    }
    return 0;
}